# 掲示板の使い方

セットアップ済みで、起動方法（`npm install` → `npm start`）がわかっている前提の、
利用者向けの操作ガイドです。導入・設定方法は [README.md](./README.md) を参照してください。

## 1. 掲示板を開く

1. アプリを起動する（初めての場合は `README.md` の手順で `npm install` → `npm start`）
2. ブラウザで `http://localhost:3000` を開く
   （ポート番号を変えている場合はその番号に合わせる）
3. 画面に他のメンバーの投稿も含めて一覧が表示されれば、共有フォルダに正しく
   つながっています

## 2. 新しく投稿する

1. ページ上部の「新規投稿」欄に入力する
   - お名前（省略可。空欄だと「匿名」と表示されます）
   - タイトル（必須）
   - 本文（必須）
2. 「投稿する」ボタンを押す
3. 一覧の一番上に投稿が追加されます

## 3. コメントを付ける

1. コメントしたい投稿の下にある「コメントする」フォームを使う
2. お名前（省略可）とコメント内容を入力して「コメントする」を押す
3. その投稿の下にコメントが追加されます

## 4. ファイルを添付する

1. 投稿の下にある「添付を追加」フォームでファイルを選択する
   （複数選択可、1ファイルあたり最大20MB）
2. 「添付を追加」ボタンを押す
3. 投稿の本文の下にファイル名がリンクとして表示されます。クリックすると
   ダウンロード・閲覧できます

## 5. 表示の更新

- 画面は15秒ごとに自動で最新の状態に更新されます
- すぐに最新の投稿・コメントを見たい場合はブラウザの再読み込み（更新）で
  即座に反映されます

## できないこと（現時点の仕様）

- 投稿・コメントの編集や削除はできません
- 誰でも他の人になりすまして投稿できてしまうため、社内など信頼できる
  メンバー間での利用を想定しています（ログイン機能はありません）

## うまく表示されない・他の人の投稿が見えないとき

- 共有フォルダ（`config.json` の `dataDir` で指定した場所）に自分のPCから
  アクセスできているか確認してください
- 全員が同じ共有フォルダのパスを指定してアプリを起動しているか確認してください
- アプリを起動し直す（`npm start` をやり直す）と直ることがあります
